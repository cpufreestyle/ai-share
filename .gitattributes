* text=auto

# Shell scripts must keep LF, otherwise the shebang breaks on Linux/macOS.
*.sh text eol=lf

# Windows scripts keep CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binary artifacts must never be line-ending converted.
*.exe binary
*.png binary
*.ico binary
